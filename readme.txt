If you want to run the program, just run the start.bat or the start.sh script.
If you look into the script, you can change how many runs should be executed with the -c option
and you can change how long each run takes with the -t option!